import React from 'react';

export interface ModalProps {
  children?: React.ReactNode;
  open: boolean;
}

export interface ModalTileProps {
  children?: React.ReactNode;
}

export interface ModalActionsProps {
  children?: React.ReactNode;
}
